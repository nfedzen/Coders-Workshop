
const autocomplete = (string, set) => {
let complete = set.forEach(word => {
  let splitWord = word.split('')
  let splitString = string.split('')
  counter = 0;
  while (splitWord.length() > counter){
    
  }
});
}